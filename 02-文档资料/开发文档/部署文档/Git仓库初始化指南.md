# Git 仓库初始化指南

## 🚀 快速开始

### 方法 1: 使用脚本（推荐）

```bash
cd "/Users/luyu/CascadeProjects/Sandy的AI收藏夹"
./初始化Git仓库.sh
```

### 方法 2: 手动初始化

```bash
cd "/Users/luyu/CascadeProjects/Sandy的AI收藏夹"

# 1. 初始化 Git 仓库
git init

# 2. 添加所有文件
git add .

# 3. 创建初始提交
git commit -m "初始提交: Sandy的AI收藏夹项目"
```

---

## 📋 连接 GitHub 仓库

### 步骤 1: 在 GitHub 创建新仓库

1. 访问 https://github.com/new
2. 填写仓库名称（例如：`sandy-ai-collection`）
3. 选择 **Private** 或 **Public**
4. **不要** 初始化 README、.gitignore 或 license（我们已经有了）
5. 点击 "Create repository"

### 步骤 2: 连接本地仓库到 GitHub

```bash
cd "/Users/luyu/CascadeProjects/Sandy的AI收藏夹"

# 添加远程仓库（替换 <你的GitHub用户名> 和 <仓库名>）
git remote add origin https://github.com/<你的GitHub用户名>/<仓库名>.git

# 重命名主分支为 main（如果还没有）
git branch -M main

# 推送到 GitHub
git push -u origin main
```

### 步骤 3: 验证

访问你的 GitHub 仓库页面，应该能看到所有文件都已上传。

---

## 🔒 重要：确保私有内容不被提交

项目已经配置了 `.gitignore`，会自动排除：

- ✅ `01-源代码/内容源/私有笔记/` - 私有笔记不会被提交
- ✅ `node_modules/` - 依赖不会被提交
- ✅ `.contentlayer/` - 构建产物不会被提交
- ✅ `.env*.local` - 环境变量不会被提交

### 验证 .gitignore 是否生效

```bash
# 检查哪些文件会被提交
git status

# 应该看不到私有笔记目录
```

---

## 📝 日常使用

### 提交更改

```bash
# 查看更改
git status

# 添加更改
git add .

# 提交
git commit -m "描述你的更改"

# 推送到 GitHub
git push
```

### 查看提交历史

```bash
git log --oneline
```

---

## 🆘 常见问题

### 问题 1: 推送时要求输入用户名密码

**解决方案**: 使用 Personal Access Token 或配置 SSH

**使用 Personal Access Token**:
1. 访问 https://github.com/settings/tokens
2. 生成新的 token（选择 `repo` 权限）
3. 推送时使用 token 作为密码

**配置 SSH**:
```bash
# 生成 SSH 密钥
ssh-keygen -t ed25519 -C "your_email@example.com"

# 添加 SSH 密钥到 GitHub
# 1. 复制公钥: cat ~/.ssh/id_ed25519.pub
# 2. 在 GitHub Settings > SSH and GPG keys 中添加

# 使用 SSH URL 连接
git remote set-url origin git@github.com:<用户名>/<仓库名>.git
```

### 问题 2: 不小心提交了私有内容

**解决方案**:
```bash
# 从 Git 历史中移除文件（但保留本地文件）
git rm --cached "01-源代码/内容源/私有笔记/某个文件.md"

# 更新 .gitignore
# 确保私有笔记目录在 .gitignore 中

# 提交更改
git commit -m "移除私有内容"

# 推送到 GitHub
git push
```

### 问题 3: 需要更新 .gitignore

如果 `.gitignore` 文件需要更新：

```bash
# 编辑 .gitignore
# 添加需要忽略的文件或目录

# 如果已经提交了应该忽略的文件，需要从 Git 中移除
git rm -r --cached <文件或目录>
git commit -m "更新 .gitignore"
git push
```

---

## ✅ 检查清单

在推送到 GitHub 之前，确保：

- [ ] `.gitignore` 已正确配置
- [ ] 私有笔记目录不会被提交
- [ ] 环境变量文件不会被提交
- [ ] `node_modules/` 不会被提交
- [ ] 已创建初始提交
- [ ] 已连接到 GitHub 远程仓库

---

## 📚 相关文档

- [Git 官方文档](https://git-scm.com/doc)
- [GitHub 文档](https://docs.github.com)
- [部署准备指南](./部署准备指南.md)

